# /etc/rc.conf
[services]

# keeps CLOCK_REALTIME synchronized after the network becomes available
ntpd:/bin/ntpd --quiet

# # authenticates and starts shell from /etc/passwd
# login:/bin/login

# desktop workspace
weston:/usr/bin/startw
